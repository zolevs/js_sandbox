const ps = document.querySelectorAll('p');
console.log(ps);
ps.forEach(function (p){
    // p.remove();
    // console.log(p.textContent);
    p.textContent = '*******'
})

// const ps = document.querySelectorAll('p');
// ps.remove()